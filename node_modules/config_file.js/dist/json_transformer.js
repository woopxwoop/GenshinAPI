"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.JsonTransformerChain = exports.JsonTransformer = void 0;
class JsonTransformer {
    constructor(jsonOptions) {
        this.jsonOptions = jsonOptions;
    }
}
exports.JsonTransformer = JsonTransformer;
class JsonTransformerChain extends JsonTransformer {
    constructor(jsonOptions, transformers) {
        super(jsonOptions);
        this.transformers = transformers;
    }
    transform(value) {
        let transformedValue = value;
        for (const transformer of this.transformers) {
            transformedValue = transformer.transform(transformedValue);
        }
        return transformedValue;
    }
}
exports.JsonTransformerChain = JsonTransformerChain;
